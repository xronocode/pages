remote_theme: just-the-docs/just-the-docs
title: xronocode
description: Заметки и дискуссии

color_scheme: light

plugins:
  - jekyll-remote-theme

nav_enabled: true

defaults:
  - scope:
      path: ""
    values:
      layout: default
